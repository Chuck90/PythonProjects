#If the bill was $150.00, split between 5 people, with 12% tip. 
#Each person should pay (150.00 / 5) * 1.12 = 33.6
#Format the result to 2 decimal places = 33.60

print("Welcome to the print calculator")
total=input("What was the total ammount of the bill? ")
percent = input("What percentage tip would you like to give? 10, 12 or 15? ")
ppl= input("How many people to split the bill? ")

porcentaje=((float(percent)/100)+1)

resultado=(float(total)*float(porcentaje))/int(ppl)
final=round(resultado, 2)
print (final)
